The initial purpose of ZFSpy is writing a python module which can 
walk through the zfs filesystem, working with zfs-fuse.

/etc/zfs/zpool.cache
    stores all the data of zpool configuration, nvlist format 
